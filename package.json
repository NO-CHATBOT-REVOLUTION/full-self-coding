{
  "name": "full-self-coding",
  "version": "1.0.1",
  "description": "A CLI tool for automated code analysis and task execution",
  "module": "src/main.ts",
  "type": "module",
  "bin": {
    "full-self-coding": "./src/main.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build ./src/main.ts --target bun --outdir dist",
    "prepare": "bun run build",
    "start": "bun src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "toml": "^3.0.0",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}